# Broker embarqué pour les tests : pas besoin d'un Kafka réel au démarrage du contexte Spring.
spring.kafka.bootstrap-servers=${spring.embedded.kafka.brokers}
# Même topic que dans application.properties et que @EmbeddedKafka(topics = ...).
app.kafka.messages-topic=mon-tunnel-topic
